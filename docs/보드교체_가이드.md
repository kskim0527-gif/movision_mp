# 보드 교체 방법 가이드

> **대상 보드 조합**: `movision_ws` ↔ `movision / movition-0223`  
> 두 보드 간에 LCD 구성을 번갈아 가며 펌웨어를 올릴 때 반드시 아래 내용을 체크하고 수정하세요.

---

## ✅ 체크리스트

- [ ] 1. GPIO 핀맵 변경 (`main/main.c`)
- [ ] 2. Full Clean → Build → Flash

> **💡 LCD 드라이버(`components/esp_lcd_sh8601`)는 두 보드 모두 완전히 동일**하므로 교체 불필요! (MD5 해시 검증 완료)

---

## 1. 📍 GPIO 핀맵 변경 (`main/main.c`)

`main.c` 상단의 매크로 핀 번호를 대상 보드에 맞게 통째로 교체합니다.

### 🔹 movision (0223 보드) 용 핀맵

```c
// LCD Pin Definitions
#define PIN_NUM_LCD_CS    (GPIO_NUM_14)
#define PIN_NUM_LCD_PCLK  (GPIO_NUM_7)
#define PIN_NUM_LCD_DATA0 (GPIO_NUM_8)   // D0
#define PIN_NUM_LCD_DATA1 (GPIO_NUM_13)  // D1
#define PIN_NUM_LCD_DATA2 (GPIO_NUM_6)   // D2
#define PIN_NUM_LCD_DATA3 (GPIO_NUM_12)  // D3
#define PIN_NUM_LCD_RST   (GPIO_NUM_9)
#define PIN_NUM_LCD_VCI_EN (GPIO_NUM_18) // LCD Power Enable
// Touch Pins
#define PIN_TOUCH_SDA GPIO_NUM_10
#define PIN_TOUCH_SCL GPIO_NUM_17
#define PIN_TOUCH_INT GPIO_NUM_11
#define PIN_TOUCH_RST GPIO_NUM_15
```

### 🔸 movision_ws (기존 구성) 용 핀맵

```c
// LCD Pin Definitions
#define PIN_NUM_LCD_CS    (GPIO_NUM_12)
#define PIN_NUM_LCD_PCLK  (GPIO_NUM_38)
#define PIN_NUM_LCD_DATA0 (GPIO_NUM_4)   // D0
#define PIN_NUM_LCD_DATA1 (GPIO_NUM_5)   // D1
#define PIN_NUM_LCD_DATA2 (GPIO_NUM_6)   // D2
#define PIN_NUM_LCD_DATA3 (GPIO_NUM_7)   // D3
#define PIN_NUM_LCD_RST   (GPIO_NUM_39)
#define PIN_NUM_LCD_VCI_EN (GPIO_NUM_18) // LCD Power Enable
// Touch Pins
#define PIN_TOUCH_SDA GPIO_NUM_15
#define PIN_TOUCH_SCL GPIO_NUM_14
#define PIN_TOUCH_INT GPIO_NUM_11
#define PIN_TOUCH_RST GPIO_NUM_40
```

---

## 2. 🗂️ LCD 드라이버 소스 파일 (`components/esp_lcd_sh8601`)

✅ **교체 불필요!**

`movition_0223`과 `movition_ws`의 `esp_lcd_sh8601` 드라이버 파일을 **MD5 해시로 전수 검증한 결과, 14개 파일 모두 완전히 동일**합니다.

| 파일 | 동일 여부 |
|------|----------|
| `esp_lcd_sh8601.c` | ✅ 동일 |
| `include/esp_lcd_sh8601.h` | ✅ 동일 |
| `CMakeLists.txt` | ✅ 동일 |
| `idf_component.yml` | ✅ 동일 |
| 기타 전체 (14개) | ✅ 모두 동일 |

**→ 보드 교체 시 드라이버 파일 복사/교체 작업은 불필요합니다. 핀맵만 변경하면 됩니다.**

---

## 3. 🧹 Full Clean 후 반드시 재빌드

> 코드를 올바르게 수정했어도, 캐시가 남아 있으면 화면이 나오지 않을 수 있습니다!

**방향 전환 시 무조건 아래 순서로 진행:**

```
1. Full Clean  (VS Code 하단 휴지통 아이콘 클릭)
2. Build
3. Flash
```

---

## 📋 빠른 비교표

| 항목 | movision (0223) | movision_ws |
|------|----------------|-------------|
| LCD CS | GPIO_NUM_14 | GPIO_NUM_12 |
| LCD PCLK | GPIO_NUM_7 | GPIO_NUM_38 |
| LCD DATA0 | GPIO_NUM_8 | GPIO_NUM_4 |
| LCD DATA1 | GPIO_NUM_13 | GPIO_NUM_5 |
| LCD DATA2 | GPIO_NUM_6 | GPIO_NUM_6 |
| LCD DATA3 | GPIO_NUM_12 | GPIO_NUM_7 |
| LCD RST | GPIO_NUM_9 | GPIO_NUM_39 |
| LCD VCI_EN | GPIO_NUM_18 | GPIO_NUM_18 |
| Touch SDA | GPIO_NUM_10 | GPIO_NUM_15 |
| Touch SCL | GPIO_NUM_17 | GPIO_NUM_14 |
| Touch INT | GPIO_NUM_11 | GPIO_NUM_11 |
| Touch RST | GPIO_NUM_15 | GPIO_NUM_40 |
| LCD 드라이버 | ✅ 두 보드 동일 (교체 불필요) | ✅ 두 보드 동일 (교체 불필요) |
